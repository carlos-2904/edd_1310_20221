class Array:
    def __init__( self , tam ):
        self.__info = [ 0 for x in range(tam)]

    def get_item( self , posicion ):

        dato = 0
        try:
            dato = self.__info[ posicion ]
        except Exception as e:
            print("Error de posicion")
        return dato

    def  get_length( self ):
        return len( self.__info ):
    def clear( self , dato ):
        self.__info = [ dato for x in range(len(self.__info))

class IteradorArreglo:
    def __init__( self , arr ):
        selr.__arr = arr
        self.__indice = 0

    def __iter__( self ):
        return self
    def __next__( self ):

    


algo = Array(10)
print(algo.get_item(6363))
algo.set_item(555)
print(algo.get_item(3))
print( f"El arrglo tiene { algo.get_item() } elementos")
